apiVersion: v1
kind: Service
metadata:
  name: {{ include "hypernym.fullname" . }}
  labels:
    {{- include "hypernym.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "hypernym.selectorLabels" . | nindent 4 }}
